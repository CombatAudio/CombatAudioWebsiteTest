---
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} >
    <title>Astro</title>
 <head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!--Code below for index structure from chat gpt-->
  <title>Recording Services</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    html, body {
      height: 100%;
      font-family: 'Arial', sans-serif;
      overflow-x: hidden;
    }

    /* HEADER */
    header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      height: 70px;
      background-color: #111;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0 1.5rem;
      z-index: 1000;
    }

    .logo {
      font-size: 1.2rem;
      font-weight: bold;
    }

    .menu-icon {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      height: 1.2rem;
      cursor: pointer;
    }

    .menu-icon div {
      width: 25px;
      height: 3px;
      background-color: white;
    }

    /* NAV PANEL */
    .nav-panel {
      position: fixed;
      top: 70px;
      right: -250px;
      width: 250px;
      height: calc(100% - 70px);
      background-color: #222;
      color: white;
      transition: right 0.3s ease;
      padding: 1rem;
      z-index: 999;
    }

    .nav-panel.active {
      right: 0;
    }

    .nav-panel a {
      display: block;
      margin: 1rem 0;
      color: white;
      text-decoration: none;
    }

    /* MAIN LAYOUT */
    main {
      display: flex;
      flex-direction: row;
      margin-top: 70px;
      height: calc(100vh - 70px);
    }

    section {
      flex: 1;
      color: white;
      font-size: 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      background-size: cover;
      background-position: center;
      transition: transform 0.3s;
    }

    #live {
      background-image: url('https://images.unsplash.com/photo-1544716278-ca5e3f4abd8c?auto=format&fit=crop&w=800&q=80');
    }

    #studio {
      background-image: url('https://images.unsplash.com/photo-1586880244401-ea9f3b720f2b?auto=format&fit=crop&w=800&q=80');
    }

    #field {
      background-image: url('https://images.unsplash.com/photo-1619983081563-3a8bff9fa53f?auto=format&fit=crop&w=800&q=80');
    }

    /* RESPONSIVE (STACK ON SMALL SCREENS) */
    @media (max-width: 768px) {
      main {
        flex-direction: column;
      }
      section {
        height: calc((100vh - 70px) / 3);
      }
    }
  </style>
</head>
<body>

  <!-- HEADER -->
  <header>
	<div id="navPanel" class="nav-panel">
    <div class="logo">Recording Services</div>
    <div class="menu-icon" onclick="toggleNav()">
      <div></div>
      <div></div>
      <div></div>
    </div>
  </header>

  <!-- NAV PANEL -->
  <div id="navPanel" class="nav-panel">
    <a href="#live">Live Recording</a>
    <a href="#studio">Studio Recording</a>
    <a href="#field">Field Recording</a>
  </div>

  <!-- MAIN CONTENT -->
  <main>
    <section id="live">
      Live Recording
    </section>
    <section id="studio">
      Studio Recording
    </section>
    <section id="field">
      Field Recording
    </section>
  </main>

  <!-- JS -->
  <script>
    function toggleNav() {
      const panel = document.getElementById('navPanel');
      panel.classList.toggle('active');
    }
  </script>

  <script>
  document.addEventListener('DOMContentLoaded', function () {
    const menuIcon = document.querySelector('.menu-icon');
    const navPanel = document.getElementById('navPanel');

    menuIcon.addEventListener('click', function () {
      navPanel.classList.toggle('active');
    });
  });
</script>

</body>
</html>


<!-- ---
import Welcome from '../components/Welcome.astro';
import Layout from '../layouts/Layout.astro';

// Welcome to Astro! Wondering what to do next? Check out the Astro documentation at https://docs.astro.build
// Don't want to use any of this? Delete everything in this file, the `assets`, `components`, and `layouts` directories, and start fresh.
---

<Layout>
	<Welcome />
</Layout> -->
